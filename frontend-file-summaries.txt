{
  "frontend/src/index.css": "Global dark-themed styles with OKLCH color system featuring deep charcoal backgrounds, red-orange destructive accents, teal positive accents, and Inter/Space Grotesk typography.",
  "frontend/tailwind.config.js": "Extended Tailwind config with custom dark palette, Space Grotesk display font, positive/warning color tokens, and card shadow utilities.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations; re-adds `useIsCallerAdmin` (calls `actor.isCallerAdmin()`), `useGetCurrentUser` as an alias for `useGetUser`, and `useGetPendingAppeals`, plus the three new admin response mutation hooks and all other existing hooks.",
  "frontend/src/components/Layout.tsx": "Persistent app shell with sticky header; adds a Settings navigation link (desktop and mobile) visible only to authenticated users, linking to the /settings route.",
  "frontend/src/components/ProfileSetupModal.tsx": "Modal dialog for first-time login profile setup, collecting name and username, shown only when authenticated user has no profile.",
  "frontend/src/pages/HomePage.tsx": "Landing page hero with feature cards and role explanation; uses `useIsCallerAdmin` and compares `userRole` to `UserRole.user` enum value (fixing the removed `useIsCallerAdmin` import error).",
  "frontend/src/pages/AdminPanel.tsx": "Admin-only panel with a 5-tab interface (Moderators, Reports, Appeals, Applications, Users); each report/appeal/application card now has an inline \"Type your response\" / \"Add a response\" toggle that reveals a textarea and submit button, with existing admin responses displayed read-only; the new Moderator Applications tab lists all applications with status badges and search-detection warnings.",
  "frontend/src/pages/ModeratorDashboard.tsx": "Moderator dashboard with searchable/filterable user list, per-user inline action forms for warnings and bans, and one-click instant ban for pornographic content.",
  "frontend/src/pages/UserStatus.tsx": "User-facing status page showing ban status, admin appeal response, appeal submission form, warning history, and moderator reporting form; updated to import `useGetUser` instead of the removed `useGetCurrentUser`.",
  "frontend/src/pages/AccessDenied.tsx": "Simple access denied page shown when unauthorized users attempt to access restricted routes.",
  "frontend/src/App.tsx": "Main app with TanStack Router; adds the /settings route wrapped in AuthGuard rendering SettingsPage, and mounts FloatingMusicPlayer inside RootLayout so it persists across all route changes.",
  "frontend/src/components/ModeratorApplicationModal.tsx": "Multi-step Dialog modal for the moderator application flow; now presents a 5-question Sonic-themed multiple-choice survey (no Principal ID fields), with search detection active on all survey and fallback steps, a once-per-session warning via useRecordSearchAttempt, and the preserved \"Can't Apply?\" fallback path (Sega Genesis True/False question).",
  "frontend/src/components/PrincipalIdGenerator.tsx": "New component that generates a scrambled 16-digit numeric-only Principal ID (formatted as XXXX-XXXX-XXXX-XXXX) with a \"Generate Principal ID\" button and an adjacent copy-to-clipboard button with toast feedback, visible to all users.",
  "frontend/src/hooks/useMusicPlayer.ts": "Custom hook managing YouTube IFrame API integration and music player state (track index, volume, enabled/disabled, custom tracks), now with fully self-contained inline TypeScript type definitions for the YouTube IFrame API instead of relying on the missing `@types/youtube` package, persisting state to sessionStorage and syncing across multiple hook instances via an in-memory event emitter.",
  "frontend/src/components/FloatingMusicPlayer.tsx": "Persistent floating music player widget fixed to the bottom-right corner, rendering only for authenticated users; provides track navigation arrows, power toggle, volume slider with nudge buttons, and a collapse toggle, all wired to the useMusicPlayer hook.",
  "frontend/src/pages/SettingsPage.tsx": "Settings page with a Background Music section that lists all Sonic tracks and custom tracks with Select/Active indicators, a volume slider with nudge buttons, a power toggle, and a YouTube URL import form with validation and error/success feedback."
}